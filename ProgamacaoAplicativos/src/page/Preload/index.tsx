import React from 'react';
import { Platform, RefreshControl } from 'react-native';
import { Container, HeaderTitle } from './styles';


export default () => {
    return (
        <Container>
            <HeaderTitle>Preload</HeaderTitle>
        </Container>
    );
}